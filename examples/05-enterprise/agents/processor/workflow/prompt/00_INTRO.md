# Data Processing Task

Process the ingested data from the ingester agent. Apply cleaning, normalization, and enrichment transformations. Calculate a quality score and write the processing summary to memory. Alert the reporter if data quality is below threshold.
