# Copy to secrets.yaml and fill in actual values
# These are injected into MCP tools at runtime -- LLM never sees them
secrets:
  # SEARCH_API_KEY: "your-search-api-key"
  # AUTH_TOKEN: "{{ env.PROD_AUTH_TOKEN }}"
